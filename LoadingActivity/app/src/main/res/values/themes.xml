<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.LoadingActivity" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <item name="colorPrimary">@color/loadingImgColor</item>
    </style>

    <style name="Base.Theme.LoginActivity" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <item name="colorPrimary">#999999</item>
    </style>

    <style name="Theme.LoadingActivity" parent="Base.Theme.LoadingActivity" />
    <style name="Theme.LoginActivity" parent="Base.Theme.LoginActivity" />
</resources>